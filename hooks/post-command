#!/usr/bin/env bash
set -euo pipefail
[[ "${BUILDKITE_PLUGIN_DOCKER_RUN_HOOK:-command}" != "post-command" ]] && exit 0
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${DIR}/../lib/run.bash"
